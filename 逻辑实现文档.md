# TokenBank Chainlink Automation 逻辑实现文档

## 概述

TokenBank 是一个基于 ERC20 代币的存款银行合约。用户可以通过 `deposit()` 存入代币，通过 `withdraw()` 提取代币。本次实现为 TokenBank 添加了 **Chainlink Automation** 自动化任务：当合约中存款总额超过可自定义的阈值 X 时，自动将一半存款转移给 Owner。

## 架构图

```
┌──────────────────────────────────────────────────────────────┐
│                      Chainlink Network                        │
│  ┌─────────────┐     eth_call       ┌──────────────────────┐ │
│  │  Automation  │──────────────────▶│  TokenBank            │ │
│  │  Node        │   checkUpkeep()   │  .checkUpkeep()       │ │
│  │              │◀──────────────────│                       │ │
│  │              │  (bool, bytes)    │  条件：               │ │
│  │              │                   │   totalBalance ≥ X    │ │
│  │              │                   │   AND                 │ │
│  │              │     transaction   │   block.timestamp     │ │
│  │              │──────────────────▶│   - lastUpkeepTime    │ │
│  │              │  performUpkeep()  │   ≥ minInterval       │ │
│  │              │                   │                       │ │
│  │              │                   │  执行：               │ │
│  │              │                   │  1.转移一半给owner    │ │
│  │              │                   │  2.按比例削减余额     │ │
│  └─────────────┘                   └──────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

## 核心接口

### AutomationCompatibleInterface

```solidity
interface AutomationCompatibleInterface {
    function checkUpkeep(bytes calldata checkData)
        external
        returns (bool upkeepNeeded, bytes memory performData);

    function performUpkeep(bytes calldata performData) external;
}
```

## 状态变量

| 变量 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `owner` | `address` | 部署者 | 接收自动转账的地址 |
| `threshold` | `uint256` | 10000 × 10¹⁸ | 触发自动转移的存款阈值 |
| `minInterval` | `uint256` | 3600 (1小时) | 两次 upkeep 最小间隔（秒） |
| `lastUpkeepTime` | `uint256` | 部署时间戳 | 上次执行 upkeep 的时间 |
| `depositors` | `address[]` | [] | 所有存款用户地址列表 |

## 核心逻辑

### checkUpkeep — 条件检查

```
输入：checkData (预留，未使用)
输出：upkeepNeeded (bool), performData (bytes)
```

**判断逻辑：**

```
1. totalBalance = token.balanceOf(address(this))
2. hasEnoughBalance = totalBalance >= threshold
3. timePassed = (block.timestamp - lastUpkeepTime) >= minInterval
4. upkeepNeeded = hasEnoughBalance AND timePassed AND totalBalance > 0
5. if upkeepNeeded: performData = abi.encode(totalBalance)
   else: performData = "" (空字节)
```

**触发条件（全部满足）：**

| # | 条件 | 说明 |
|---|------|------|
| 1 | `totalBalance ≥ threshold` | 合约余额达到或超过阈值 X |
| 2 | `block.timestamp - lastUpkeepTime ≥ minInterval` | 距上次执行超过最小间隔 |
| 3 | `totalBalance > 0` | 合约中有余额 |

### performUpkeep — 执行转账

```
输入：performData (checkUpkeep 返回的编码余额)
```

**执行步骤：**

```
1. totalBalance = token.balanceOf(address(this))
2. 验证 performData 解码后的余额 == totalBalance  ← 防恶意调用
3. 重新验证 totalBalance >= threshold             ← 防状态变化
4. 重新验证 (block.timestamp - lastUpkeepTime) >= minInterval
5. halfAmount = totalBalance / 2
6. 调用 _reduceDepositsProportionally(halfAmount, totalBalance)
7. 调用 token.transfer(owner, halfAmount)
8. lastUpkeepTime = block.timestamp
9. 触发 AutoSweep 事件
```

### _reduceDepositsProportionally — 按比例削减

```
输入：sweepAmount (转走金额), totalBalance (总余额)
```

**计算逻辑：**

```
remainingAfterSweep = totalBalance - sweepAmount

对 depositors 数组中每个用户：
  if depositBalances[user] > 0:
    depositBalances[user] = depositBalances[user] × remainingAfterSweep / totalBalance
```

**示例：**
- 总余额 = 20,000，转走 = 10,000
- 用户A余额 = 5,000 → 5,000 × 10,000 / 20,000 = **2,500**
- 用户B余额 = 15,000 → 15,000 × 10,000 / 20,000 = **7,500**

## 可调参数管理

### setThreshold(uint256 _newThreshold)

- 权限：仅 Owner
- 事件：`ThresholdUpdated(old, new)`
- 用途：调整触发自动转移的存款阈值

### setMinInterval(uint256 _newInterval)

- 权限：仅 Owner
- 事件：无（可后续添加）
- 用途：调整两次 upkeep 之间的最小间隔

## 事件

| 事件 | 参数 | 触发时机 |
|------|------|----------|
| `Deposit` | `user, amount` | 用户存款 |
| `Withdraw` | `user, amount` | 用户取款 |
| `AutoSweep` | `to, amount, timestamp` | performUpkeep 执行转账 |
| `ThresholdUpdated` | `oldThreshold, newThreshold` | 阈值更新 |

## 安全设计

### 1. performData 验证
`performUpkeep` 会解码 `performData` 并与当前实际余额对比，防止恶意节点传入伪造数据。

### 2. 条件重验证
即使 `checkUpkeep` 返回 `upkeepNeeded=true`，`performUpkeep` 仍重新验证所有条件，防止状态在两次调用之间发生变化（竞争条件）。

### 3. 最小间隔保护
`minInterval` 防止短时间内反复触发（例如存款在阈值附近波动时），默认 1 小时。

### 4. 整数除法处理
使用 `balance * remaining / total` 的比例计算方式，最小化取整误差。奇数余额场景下误差不超过 1 wei。

## 在 Chainlink Automation 平台注册

### 前提条件
- 合约已部署到目标链（如 Ethereum Sepolia）
- 拥有足够的 LINK 代币支付 Chainlink 节点 gas 费

### 注册步骤

1. 访问 [https://automation.chain.link](https://automation.chain.link)
2. 连接钱包，点击 "Register new Upkeep"
3. 选择 **"Custom Logic"** 触发器
4. 填入 TokenBank 合约地址
5. 填入 `checkUpkeep` 的 `checkData`（留空即可，传 `0x`）
6. 设置 gas limit（建议初始 500,000）
7. 存入 LINK 代币作为预付费
8. 确认注册

### 运行机制
- Chainlink 网络节点在每个区块（或按设定间隔）对合约执行 `eth_call` 模拟 `checkUpkeep()`
- 当返回 `upkeepNeeded=true` 时，节点发送交易调用 `performUpkeep()`
- 节点从预存的 LINK 余额中扣除 gas 费用

## 部署与验证

### 编译
```bash
forge build
```

### 运行测试
```bash
forge test
```

### 部署到 Sepolia
```bash
forge script script/DeployTokenBank.s.sol --rpc-url sepolia --broadcast
```

### 合约验证
```bash
forge verify-contract <address> src/tokenBank.sol:TokenBank \
  --chain sepolia \
  --constructor-args $(cast abi-encode "constructor(address)" <ERC20_ADDRESS>)
```

## 测试覆盖

共 21 个测试用例，位于 `test/TokenBankAutomation.t.sol`：

| 分类 | 测试 | 说明 |
|------|------|------|
| checkUpkeep | `BelowThreshold` | 余额低于阈值 → false |
| | `AboveThreshold` | 余额高于阈值 → true |
| | `ExactThreshold` | 余额等于阈值 → true |
| | `MinIntervalNotMet` | 间隔不足 → false |
| | `ZeroBalance` | 零余额 → false |
| performUpkeep | `TransfersHalf` | 验证一半余额转给 owner |
| | `ProportionalReduction_SingleUser` | 单用户余额减半 |
| | `ProportionalReduction_MultipleUsers` | 多用户余额按比例减半 |
| | `OddBalance` | 奇数余额处理 |
| | `CanBeCalledAgain` | 可多次触发 |
| | `BelowThresholdReverts` | 低于阈值时 revert |
| | `MinIntervalNotMetReverts` | 间隔不足时 revert |
| | `MismatchedDataReverts` | 伪造数据时 revert |
| 阈值管理 | `SetThreshold` | Owner 可设置阈值 |
| | `SetThreshold_OnlyOwner` | 仅 Owner 权限检查 |
| | `SetThreshold_ThenCheckUpkeep` | 阈值变化影响判断 |
| 间隔管理 | `SetMinInterval` | Owner 可设置间隔 |
| | `SetMinInterval_OnlyOwner` | 仅 Owner 权限检查 |
| 存款追踪 | `DepositorTracking` | 首次存款记录用户 |
| | `DepositorTracking_AfterSweep` | sweep 后数组不变 |
| 构造函数 | `Constructor_InitialValues` | 初始值校验 |
